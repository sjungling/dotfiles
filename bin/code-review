#!/usr/bin/env bash

git merge --abort
git reset --hard
git checkout master
git pull
git merge --no-commit --no-ff origin/$1
sonar-runner -Dproject.settings=sonar-project-JS.properties &&  open ./sonar-js/issues-report/issues-report.html
